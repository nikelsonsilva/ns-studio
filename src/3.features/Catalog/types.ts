/**
 * Catalog Feature Types (Services + Products)
 */

import type { Service, Product } from '@core/infra/types';
export type { Service, Product };

export type ViewMode = 'grid' | 'list';
export type CatalogTab = 'services' | 'products';
